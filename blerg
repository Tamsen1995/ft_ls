git add -A
git commit -m "[UPD] So there seems to be something wrong with the total size of the directory. I suspect it's because I'm adding too many files in the iteration of the total_blocks function. Also in the recursive long listing the total blocks for the root directory is not shown... "
git push origin system_link_recognition
