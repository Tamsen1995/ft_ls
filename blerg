git add -A
git commit -m "[ADD] implemented the output of files in the case of it being an input. Also made sure only truly invalid inputs would send the message of unavailability."
git push origin showing_of_files_and_no_name_when_one_dir_as_input
