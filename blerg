git add -A
git commit -m "[FIX] Turns out just changing the ac can have some serious effects on how your arguments are handled. Always make sure to check out much memory is still allocated after changing variables which may be linked to memory allocation (such as ac)"
git push origin fix/memory