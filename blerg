git add -A
git commit -m "[ADD] implement the showing of files in case of putting a file into the parameters. Also not outputting the name of a directory if it is the only input "
git push origin showing_of_files_and_no_name_when_one_dir_as_input
