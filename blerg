git add -A
git commit -m "[UPD] There seems to be a weird problem with the memory in the printing of the sys links. I have fixed most leaks, but still have some to close for the user selected inputs."
git push origin fix/memory