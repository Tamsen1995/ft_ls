git add -A
git commit -m "[FIX] I resolved the problem with the strjoin, but there is still a segfault whenever I enter a file within a directory such as libft/auteur"
git push origin single_fl/file_in_dir