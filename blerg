git add -A
git commit -m "[UPD] splitting of the outpur recursion for norm done. Next I have to make sure the total block count for each directory is shown at the time of output for the long listing format."
git push origin output/directories_formatting