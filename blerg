git add -A
git commit -m "Slowly merging the output and the allocation. 
Turns out you're not supposed to add the hidden files into the total block size
if the hidden flag isn't activated. So I had to make some modifications to the show total blocks function.
Also there is now and error in the freeling of the list I need to take care of."
git push origin restructure