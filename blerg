git add -A
git commit -m "Made a function which cuts out all the nondirectories in the args, puts the valdid inputs into a different array, and also throws an error message for the invalid inputs"
git push origin make_directory_array