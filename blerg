git add -A
git commit -m "[UPD] I fixed my weird segfault, turns out my (free_list) function is faulty. I'm gonna have to make som re-adjustments to it later. I now have to make sure the total_size of blocks isn't shown when a single file is an input"
git push origin develop
