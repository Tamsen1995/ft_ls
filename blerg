git add -A
git commit -m "[UPD] Managed the output of the total block size and also fixed the bug which would mess up the order of files being printed within the subdirectories."
git push origin output/directories_formatting