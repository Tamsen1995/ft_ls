git add -A
git commit -m "[UPD] Implemented the fifo and socket feature. Now I need to make sure that the folder name does not in the case of the folder being the sole input"
git push origin feature/fifo
