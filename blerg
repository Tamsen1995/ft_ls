git add -A
git commit -m "[UPD] implemented the sorting of the stack. Also in the output, in the case that the file is considered a hidden file it will not be sent to the output"
git push origin master