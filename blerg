git add -A
git commit -m "[ADD] implemented the prev pinter as well as the recursive freeing of the entire list at the end. Next up is going through the entire source code to make sure every other malloc is properly met with a respective free."
git push origin feature/free_list
