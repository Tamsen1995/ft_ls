git add -A
git commit -m "[UPD] I splie the alloc_list module into a more isolated flow for the handling of single files as well as directories respectively. Now the problem is that in the case of my ls not having access to a certain folder the program will simply just exit."
git push origin fix/memory